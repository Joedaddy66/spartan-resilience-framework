# Environment Configuration
# Copy this file to .env and fill in actual secrets
# DO NOT commit .env to Git

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_strong_password_here
POSTGRES_DB=spartan_db
POSTGRES_PORT=5432

# OPA Configuration
OPA_PORT=8181

# Control Plane Configuration
CONTROL_PLANE_PORT=8080

# Security: Generate with: python -c 'import secrets; print(secrets.token_hex(32))'
SAVE_HMAC_SECRET=changeme_generate_strong_secret_here

# Telemetry Calibration (adjust based on your cloud provider)
# Example rates - adjust to match your actual costs
COST_PER_1K_INPUT_TOKENS=0.001
COST_PER_1K_OUTPUT_TOKENS=0.002
ENERGY_WH_PER_1K_TOKENS=0.5
CARBON_INTENSITY_G_PER_KWH=400
COMPUTE_COST_PER_HOUR=1.00
